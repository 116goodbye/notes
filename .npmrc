engine-strict=true
legacy-peer-deps=true
# The @quartz-community scope lags badly on npmmirror — some versions 404
# there (e.g. remove-draft@0.1.0). Honor the lockfile's resolved URLs
# verbatim instead of rewriting them onto the configured mirror.
replace-registry-host=never
